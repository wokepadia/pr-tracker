{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the Review Ninja main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "keyring:allow-get-password",
    "keyring:allow-set-password",
    "sql:default",
    "sql:allow-execute"
  ]
}
